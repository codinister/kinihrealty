
export const Error = ({err}: {err: any}) => {
  return (
    <div className="errors">{err}</div>
  )
}


export const InternalError = ({err}: {err: any}) => {
  return (
    <div className="errors">{err}</div>
  )
}
